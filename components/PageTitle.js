export default function PageTitle({ children }) {
  return (
    <h1 className="darkx:text-gray-100 text-2xl font-extrabold tracking-tight text-gray-900 md:text-3xl">
      {children}
    </h1>
  )
}
